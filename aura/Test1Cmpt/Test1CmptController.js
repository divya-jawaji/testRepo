({
	buttonClick : function(component, event, helper) {
        /* var updateEvent = component.getEvent("updateExpense");
        updateEvent.setParams({ "expense": "Sampath" });
        updateEvent.fire();*/
        
        helper.helperTestMethod(component);
        
        
		alert("Clicked");
	}
})